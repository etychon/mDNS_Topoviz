name: mdns-topoviz

services:
  topoviz:
    image: mdns-topoviz:local
    container_name: mdns-topoviz
    build:
      context: .
      dockerfile: Dockerfile
    network_mode: host
    cap_add:
      - NET_BIND_SERVICE
    restart: unless-stopped
    command: ["-http", ":8765"]
